<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<%@ page import="java.util.Enumeration" %>

<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>헤더 목록 출력</title>
	</head>
	<body>
		<b>request.getParameter() 메소드 사용</b>
		name parameter : <%=request.getParameter("name") %><br>
		addr parameter : <%=request.getParameter("address") %><br>
		<br><br>

		<% 
		String[] values = request.getParameterValues("pet");
		if(values != null) {
			for(int i = 0; i < values.length; i++) {
				%><%= valuse[i]%>
			<%}
		} %>
		<br><br>
		
		
		requset.getParameterNames() 메소드<br>
		<%
		Enumeration enumData = request.getParameterNames();
		
		while(enumData.hasMoreElements()) {
			String name = (String)enumData.nextElement();
			%>
		<%=name %>
		<%} %>
		
		requset.getParameterMap() 메소드<br>
		<%
		Map parameterMap = request.getParameterMap();
		String[] nameParam = (String)parameterMap.get("name");
		
		if(nameParam != null) {
			%>
			<%name = nameParam[0]%>
		<%}
		%>
	</body>
</html>